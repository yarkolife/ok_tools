# 🔐 Custom Token Admin

## Описание

Улучшенная админка для управления токенами аутентификации API с фильтрацией, поиском и интеграцией документации.

## ✨ Новые возможности

### 1. **Фильтрация пользователей**
- ✅ **Только сотрудники** - в списке создания токенов отображаются только пользователи с `is_staff=True`
- ✅ **Фильтры по статусу** - можно фильтровать по статусу сотрудника и участника
- ✅ **Улучшенный поиск** - поиск по email, имени и фамилии

### 2. **Поиск пользователей**
- 🔍 **По email** - `admin@example.com`
- 🔍 **По имени** - `John`
- 🔍 **По фамилии** - `Smith`
- 🔍 **Комбинированный поиск** - `John admin`

### 3. **API документация**
- 📖 **Клик по "📖 API Example"** открывает модальное окно
- 📋 **Готовые примеры** - cURL, JavaScript, Python
- 📋 **Копирование в буфер** - одним кликом
- 🔗 **Прямые ссылки** на API endpoints

## 🎯 Использование

### Создание токена:

1. **Перейдите в Admin → Authentication tokens → Add token**
2. **Выберите сотрудника** из отфильтрованного списка
3. **Нажмите "SPEICHERN"** (Сохранить)
4. **Получите токен** и нажмите "📖 API Example"

### Модальное окно API:

При клике на "📖 API Example" откроется окно с:

- **Ваш токен** (с кнопкой копирования)
- **API URL** с примером лицензии
- **cURL команда** для тестирования
- **JavaScript код** для веб-приложений
- **Советы по безопасности**

### Примеры использования:

#### cURL:
```bash
curl -H "Authorization: Token YOUR_TOKEN_HERE" \
  "http://localhost:8000/licenses/api/metadata/12345/"
```

#### JavaScript:
```javascript
fetch('http://localhost:8000/licenses/api/metadata/12345/', {
  headers: {
    'Authorization': 'Token YOUR_TOKEN_HERE'
  }
})
.then(response => response.json())
.then(data => console.log(data));
```

## 🔧 Технические детали

### Файлы:
- `ok_tools/admin_custom.py` - кастомная админка токенов
- `ok_tools/static/ok_tools/css/token_admin.css` - стили
- `ok_tools/static/ok_tools/js/token_admin.js` - JavaScript функциональность

### Фильтрация:
- `formfield_for_foreignkey()` - ограничивает выбор только сотрудниками
- `StaffFilter` - фильтр по статусу сотрудника
- `MemberFilter` - фильтр по статусу участника

### Поиск:
- `search_fields` - поиск по email, имени, фамилии
- `select_related` - оптимизированные запросы к БД

### API интеграция:
- Модальное окно с готовыми примерами
- Копирование в буфер обмена
- Адаптивный дизайн для мобильных устройств

## 🛡️ Безопасность

- Токены видны только администраторам
- Автоматическое скрытие неактивных пользователей
- Советы по безопасному использованию в модальном окне
- Поддержка HTTPS в продакшене

## 📱 Совместимость

- ✅ Django Admin interface
- ✅ Мобильные устройства
- ✅ Все современные браузеры
- ✅ Светлая и темная темы админки


# Tags Input Widget

## Описание

Кастомный виджет для удобного ввода тегов в Django Admin. Заменяет стандартное JSONField на пользовательский интерфейс с:

- Четкими инструкциями по вводу
- Автоматическим разделением по запятым
- Визуальным предпросмотром тегов
- Валидацией (максимум 4 тега)
- Интерактивным удалением тегов

## Использование

### В Django Admin

Виджет автоматически используется для поля `tags` в модели `License`:

1. Откройте лицензию в Django Admin
2. В поле "Tags" введите теги через запятую:
   ```
   documentary, local, culture, news
   ```
3. Виджет покажет предпросмотр тегов в реальном времени
4. При превышении лимита в 4 тега появится предупреждение

### Программное использование

```python
from licenses.widgets import TagsInputWidget

class MyForm(forms.ModelForm):
    class Meta:
        model = MyModel
        fields = ['tags']
        widgets = {
            'tags': TagsInputWidget(),
        }
```

## Функции

### ✅ Валидация
- Максимум 4 тега
- Запрет пустых тегов
- Автоматическое удаление дубликатов
- Очистка пробелов

### ✅ Пользовательский интерфейс
- **Чистое поле по умолчанию** (не показывает "[]" или "null")
- Placeholder с примером ввода
- Помощь с инструкциями
- Визуальный предпросмотр тегов
- Интерактивное удаление (клик на ×)
- Автоматическая очистка "null" значений

### ✅ Автоматизация
- Автоматическое добавление запятых при нажатии Enter
- Обработка вставки из буфера обмена
- Ограничение длины поля (500 символов)

## Файлы

- `widgets.py` - основной виджет
- `templates/licenses/widgets/tags_input.html` - шаблон
- `static/licenses/css/tags_input.css` - стили
- `static/licenses/js/tags_input.js` - JavaScript функциональность

## API

Виджет возвращает список строк для поля `tags`:

```python
# Ввод: "documentary, local, culture"
# Результат: ["documentary", "local", "culture"]
```

## Совместимость

- Django 4.0+
- Совместим с Django Admin
- Поддерживает Bootstrap стили
- Адаптивный дизайн для мобильных устройств
